import Image from "next/image";
import Link from "next/link";
import { PageHero } from "@/components/page-hero";
import { Reveal } from "@/components/reveal";
import { ministries } from "@/lib/site";
export const metadata={title:"Connect",description:"Kids, youth, young adults, families, connection groups, and Bible study at POM."};
export default function ConnectPage(){return <main><PageHero eyebrow="Connect" title="There is a place for you." copy="Faith grows in relationship. Find people in your season of life and a next step that makes this church feel like family." image="/images/conversation.jpg"/><section className="section"><div className="shell"><div className="grid gap-4 md:grid-cols-2">{ministries.map((m,i)=><Reveal key={m.title} delay={(i%2)*.06}><article id={m.title.toLowerCase().replaceAll(" ","-")} className="group grid min-h-[600px] overflow-hidden bg-[#101010] text-white lg:grid-cols-[.9fr_1.1fr]"><div className="relative min-h-[380px]"><Image src={m.image} alt="" fill className="object-cover opacity-80 transition duration-700 group-hover:scale-[1.025]"/></div><div className="flex flex-col justify-between p-7"><div><p className="eyebrow !text-white/45">{m.kicker}</p><h2 className="mt-5 text-5xl font-black uppercase leading-[.85] tracking-[-.06em]">{m.title}</h2></div><p className="mt-12 text-sm leading-6 text-white/60">{m.body}</p></div></article></Reveal>)}</div><div className="mt-16 flex flex-col items-start justify-between gap-8 border-t border-black/15 pt-8 md:flex-row"><h2 className="text-4xl font-black uppercase tracking-[-.05em]">Not sure where to start?</h2><Link href="/contact" className="button button-accent">Talk with us ↗</Link></div></div></section></main>}
